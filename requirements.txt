# JUGGERNAUT Autonomy Engine Dependencies
# ==========================================
# Minimal dependencies - we use stdlib where possible, but some features
# require external libraries.
# The autonomy engine uses only Python standard library:
# - urllib.request for HTTP (database, APIs)
# - json for data serialization
# - http.server for health endpoint
# - threading for background tasks
# - datetime, time, os, sys for basics

# External dependencies
# - httpx: required for core/experiments.py
# - pypdf: required for /api/files/upload (PDF text extraction)
# - requests: required for streaming API calls (brain.py SSE streaming)
httpx==0.27.0

pypdf>=6.6.0

requests>=2.31.0

redis>=5.0.0

# Aider — context-aware AI code generation engine
aider-chat>=0.50.0

# Langfuse — LLM observability and tracing (optional, gracefully degrades)
langfuse>=2.0.0

# Optional (uncomment if needed):
# psycopg2-binary==2.9.9  # Direct PostgreSQL (optional)
# schedule==1.2.1         # Cron scheduling (optional)
