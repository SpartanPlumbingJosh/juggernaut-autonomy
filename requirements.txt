# JUGGERNAUT Autonomy Engine Dependencies
# ==========================================
# Minimal dependencies - we use stdlib where possible

# No external dependencies required!
# The autonomy engine uses only Python standard library:
# - urllib.request for HTTP (database, APIs)
# - json for data serialization
# - http.server for health endpoint
# - threading for background tasks
# - datetime, time, os, sys for basics

# Required for core/experiments.py
httpx==0.27.0

# Optional (uncomment if needed):
# psycopg2-binary==2.9.9  # Direct PostgreSQL (optional)
# schedule==1.2.1         # Cron scheduling (optional)
